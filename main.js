class Ship {
    constructor(length) {
        this.length = length;
        this.hits = hits;
    }
    
    hit: function () {
      this.hits += 1;
    },
    isSunk: function () {
      this.isSunk = true;
    };
}

module.exports = ship;

